# 模板、组件预处理

## 预处理发生在业务的哪个阶段

## 结合 editor 的 UI，展示 template、component、block、element 的含义

## schema，前端 fetch i18n 后的 schema，就可以在 editor 中展示对应的 label

## 动态JSON、延迟解析

## 责任链模式的设计，业务开发无需了解 preprocess 流程，只需定义、编排 handler 即可实现 schema 的校验逻辑
